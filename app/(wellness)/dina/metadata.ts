import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'DINA — your dose intelligence companion — DIOS',
  description:
    'DINA tells you when to take each medicine and warns you if the timing is unsafe.',
}
