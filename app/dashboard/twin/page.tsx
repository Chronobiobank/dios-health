import { RoutePlaceholder } from '@/components/auth/route-placeholder'

export default function DashboardTwinPage() {
  return (
    <RoutePlaceholder
      title="Body clock twin"
      path="/dashboard/twin"
      note="Patient twin view — placeholder."
    />
  )
}
