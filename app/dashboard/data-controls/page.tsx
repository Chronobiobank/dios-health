import { RoutePlaceholder } from '@/components/auth/route-placeholder'

export default function DashboardDataControlsPage() {
  return (
    <RoutePlaceholder
      title="Data controls"
      path="/dashboard/data-controls"
      note="GP, research, and policy sharing toggles — placeholder."
    />
  )
}
