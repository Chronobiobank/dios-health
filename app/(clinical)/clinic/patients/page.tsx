import { RoutePlaceholder } from '@/components/auth/route-placeholder'

export default function ClinicPatientsPage() {
  return (
    <RoutePlaceholder
      title="All patients"
      path="/clinic/patients"
      note="Patient list — placeholder."
    />
  )
}
